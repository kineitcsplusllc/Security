name: "General Security Question"
description: "Ask a non-vulnerability security question"
labels: ["question"]
body:
  - type: textarea
    id: question
    attributes:
      label: "What is your question?"
      description: "Do not include any sensitive data."
      placeholder: "Enter your question here..."
    validations:
      required: true
